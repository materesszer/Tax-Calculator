#include <iostream>
#include <string.h>
using namespace std;

int main()
{
    // Asks user for monthly income, stores value in income
    cout << "What is your monthly income? ";
    int income;
    cin >> income;

    // Asks user whether he is taxed progressively or flat
    cout << "Type p for progressive, f for flat tax method";
    string method;
    cin >> method;
}